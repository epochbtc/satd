/**
 * No translations yet. English-only is honest; machine-translated operator
 * text that nobody who speaks the language has read is not, and a wrong
 * string here is one a user acts on.
 *
 * To add one, map the numeric ids from ./default.ts — the ids are the
 * contract, so the English text can be reworded without touching this file.
 */
export default {} as const
