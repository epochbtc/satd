/target
*.swp
.idea/
.vscode/
.cargo/config.toml

# Nix build outputs (`nix build` symlinks; flake repl history)
result
result-*
.direnv/

# CycloneDX SBOMs from local `cargo cyclonedx` runs. CI generates and
# uploads these as release artifacts; never check them in.
*.cdx.json
*.cdx.json.sha256
